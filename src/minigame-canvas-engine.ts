import Layout from 'minigame-canvas-engine'
// var Layout: any;
// if (process.env.NODE_ENV === 'production') {
// } else {
//     Layout = requirePlugin('Layout');
// }

// Layout = await import('minigame-canvas-engine')
export default Layout;
